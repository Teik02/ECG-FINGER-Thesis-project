/**
 * @brief Sets the charging mode.
 *
 * @param chgMode The charging mode (1 for fast charge, 0 for normal charge).
 */
void setChargingMode(int chgMode) {
  // if (chgMode) {
  //     digitalWrite(HICHG, LOW);
  // } else {
  //     digitalWrite(HICHG, HIGH);
  // }
}